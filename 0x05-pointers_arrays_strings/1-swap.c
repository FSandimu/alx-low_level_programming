#include "main.h"
/**
 * swap_int - swaps the values of two integers
 * @a: first integer
 * @b: second integer
 * Return: void(success)
 */
void swap_int(int *a, int *b)
{
	int swap;
	change = *b;
	*b = *a;
	*a = change;
}
